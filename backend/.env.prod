# Production Environment Configuration
# Copy this to .env.prod.live and update with your actual AWS credentials and URLs

# ============================================
# REQUIRED FOR PRODUCTION
# ============================================

# Deployment Environment
ENVIRONMENT=production

# AWS Aurora PostgreSQL Database (REQUIRED)
# Format: postgresql://username:password@host:port/database_name
# Example: postgresql://admin:MyPassword123@order-mgmt.c7jxyz.us-east-1.rds.amazonaws.com:5432/order_management
DATABASE_URL=

# AWS Credentials for Bedrock (REQUIRED)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1

# ============================================
# AIRFLOW CONFIGURATION (REQUIRED)
# ============================================

# Airflow Fernet Key - Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW__CORE__FERNET_KEY=

# Airflow Webserver Secret Key - Generate any strong random string
AIRFLOW__WEBSERVER__SECRET_KEY=

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Email Configuration (for email notifications)
EMAIL=your_email@gmail.com
EMAIL_PASSWORD=your_app_password

# FastAPI Configuration
API_BASE_URL=http://fastapi-app:8000/v1
ENQUIRY_BASE_URL=https://your-domain.com

# Logging Level
LOG_LEVEL=INFO

# ============================================
# INSTRUCTIONS
# ============================================
# 1. Replace DATABASE_URL with your AWS Aurora endpoint
# 2. Replace AWS credentials with real values
# 3. Generate AIRFLOW__CORE__FERNET_KEY and AIRFLOW__WEBSERVER__SECRET_KEY
# 4. Rename this file to .env.prod.live before deploying
# 5. Add .env.prod.live to .gitignore to prevent credential leaks
